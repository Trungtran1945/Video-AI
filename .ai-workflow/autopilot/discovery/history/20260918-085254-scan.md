# Autonomous Project Scan

## Scan Time
2026-09-18T01:53:00Z

## Project Health
The project maintains well-structured execution pipelines across dual modes (`TRANSLATE_DUB` and `SUMMARY`), supported by BullMQ queues and SQLite persistence. However, multiple operational and consistency bugs exist: unnormalized mode strings in `runner.js` cause projects with modes such as `translate-dub` to skip all pipeline stages and immediately mark the project as 100% completed without generating any outputs; un-scoped rate-limit queries in `quotaGuardService` trigger cross-tenant throttling; synchronous directory scans in `dubRender` introduce redundant I/O during segment resolution; and `generation.js` fails stage resolution on unnormalized retry queries. Addressing these defects will significantly improve runtime correctness and pipeline stability.

## Findings

### BUG
- [`runner.js:477,567`](file:///D:/E/Video_AI/backend/src/pipeline/runner.js#L477) â€” `const stageGroups = STAGES[project.mode] || []` looks up `STAGES` with unnormalized mode strings. If `project.mode` is lowercase or hyphenated (`translate-dub`), `stageGroups` evaluates to `[]`, bypassing all stage jobs and immediately setting `projects.status = 'completed'` and `progress: 100` at line 567 without generating any audio or video output.
- [`projects.js:317`](file:///D:/E/Video_AI/backend/src/routes/v1/projects.js#L317) â€” `firstRunnableStage(STAGES[project.mode] || [], jobs)` passes unnormalized `project.mode` into `STAGES`, causing stage resolution to fail on lowercase or hyphenated modes during pipeline regeneration.
- [`generation.js:82`](file:///D:/E/Video_AI/backend/src/routes/v1/generation.js#L82) â€” `firstRunnableStage(STAGES[req.project.mode] || [], jobs)` in the retry endpoint passes raw unnormalized mode strings into `STAGES`, resolving to `undefined` and preventing stage resume.

### SECURITY
- [`quotaGuardService.js:109-114`](file:///D:/E/Video_AI/backend/src/services/quotaGuardService.js#L109-L114) â€” `selectBestApiKey(userId, provider)` queries `provider_logs` for `status = 'rate_limited'` across all users globally without user scoping. A single user hitting a rate limit causes all active API keys for all users on that provider to be skipped for 15 minutes.

### PERFORMANCE
- [`dubRender.js:87-92`](file:///D:/E/Video_AI/backend/src/pipeline/stages/dubRender.js#L87-L92) â€” `scanForSegment(segmentId)` invokes `fs.readdirSync(segDir)` on every call during audio entry matching, causing $O(N)$ repeated synchronous filesystem directory reads instead of inspecting a cached list or existing map.
- [`dubTtsAlign.js:206-210`](file:///D:/E/Video_AI/backend/src/pipeline/stages/dubTtsAlign.js#L206-L210) â€” `segments.find()` is called twice per element inside `fitted.map()`, causing quadratic search overhead during timeline alignment.

### TEST
- [`backend/tests/`](file:///D:/E/Video_AI/backend/tests) â€” Automated test coverage for `SUMMARY` mode pipeline stages (`summaryScript`, `summaryAlign`, `summarySubtitle`, `summaryRender`) is completely absent.

### REFACTOR
- [`callProvider.js:187-196`](file:///D:/E/Video_AI/backend/src/lib/callProvider.js#L187-L196) â€” `callProvider` receives `apiKeyId` but `logCall` omits it from `provider_logs` inserts, hindering per-key audit and debugging.

### FEATURE OPPORTUNITY
- [`Queue.jsx:16-27`](file:///D:/E/Video_AI/frontend/src/pages/Queue.jsx#L16-L27) â€” The Queue page polls job statuses only once on mount; integrating SSE progress events via `GET /projects/:id/events` would provide realtime pipeline progress updates.

## Recommended Tasks
1. TASK-029 Normalize project mode before looking up STAGES in generation retry endpoint
2. TASK-030 Normalize project mode and guard against empty stage groups in pipeline runner and regenerate endpoint
3. TASK-031 Isolate rate limit query by user in quotaGuardService to prevent cross-user key throttling
4. TASK-032 Cache directory entries in dubRender audio resolver to eliminate repeated synchronous filesystem scans

## Final Status
FINAL_STATUS: SCAN_DONE

# TASK-029

## Title
Normalize project mode before looking up STAGES in generation retry endpoint

## Type
bug

## Priority
medium

## Autonomy
auto

## Evidence
[`generation.js:82`](file:///D:/E/Video_AI/backend/src/routes/v1/generation.js#L82): In `POST /api/v1/projects/:id/jobs/:type/retry`, `const r = firstRunnableStage(STAGES[req.project.mode] || [], jobs)` passes `req.project.mode` verbatim to `STAGES`. While line 72 properly normalizes `req.project.mode` with `.toUpperCase().replace('-', '_')`, line 82 does not. If `req.project.mode` in the database is lowercase or hyphenated (`translate-dub`), `STAGES[req.project.mode]` returns `undefined`, evaluating to `[]` and causing `firstRunnableStage` to return no runnable stage.

## Affected Areas
- `backend/src/routes/v1/generation.js`

## Expected Outcome
Normalize the mode before indexing `STAGES`: `STAGES[String(req.project.mode || '').toUpperCase().replace('-', '_')] || []`.

## Constraints
Preserve existing validation error responses for unsupported modes.

## Suggested Verification
`cd backend && node -e "import('./src/routes/v1/generation.js')"`

## Status
PENDING

# TASK-030

## Title
Normalize project mode and guard against empty stage groups in pipeline runner and regenerate endpoint

## Type
bug

## Priority
high

## Autonomy
auto

## Evidence
[`runner.js:477,567`](file:///D:/E/Video_AI/backend/src/pipeline/runner.js#L477) and [`projects.js:317`](file:///D:/E/Video_AI/backend/src/routes/v1/projects.js#L317): In `runPipeline`, `const stageGroups = STAGES[project.mode] || []` evaluates without mode string normalization. If `project.mode` is lowercase or hyphenated (`translate-dub`), `STAGES[project.mode]` returns `undefined`, falling back to `[]`. The loop `for (const group of stageGroups)` executes 0 iterations, and execution jumps directly to line 567: `await updateById('projects', projectId, { status: 'completed', progress: 100 })`, marking the project 100% completed without generating media outputs or executing any stages. Similarly, `projects.js:317` queries `STAGES[project.mode] || []` without normalization, causing `firstRunnableStage` to fail.

## Affected Areas
- `backend/src/pipeline/runner.js`
- `backend/src/routes/v1/projects.js`

## Expected Outcome
Normalize `project.mode` in `runPipeline` and `projects.js` via `String(project.mode || '').toUpperCase().replace('-', '_')`. In `runPipeline`, explicitly verify that `stageGroups.length > 0`; if empty or unrecognized, throw an error and fail the pipeline rather than marking the project as completed.

## Constraints
Maintain full compatibility with both `SUMMARY` and `TRANSLATE_DUB` modes and their respective stage configurations.

## Suggested Verification
`cd backend && node -e "import('./src/pipeline/runner.js')"`

## Status
PENDING

# TASK-031

## Title
Isolate rate limit query by user in quotaGuardService to prevent cross-user key throttling

## Type
security

## Priority
high

## Autonomy
extra-verification

## Evidence
[`quotaGuardService.js:109-114`](file:///D:/E/Video_AI/backend/src/services/quotaGuardService.js#L109-L114): In `selectBestApiKey(userId, provider)`, the query `SELECT COUNT(*) as cnt FROM provider_logs WHERE provider = ? AND status = 'rate_limited' AND created_date >= ?` lacks tenant filtering. If any user encounters a 429 rate limit on a given provider, `recentLimit?.cnt > 0` evaluates to true for all users, skipping valid API keys for every user across the system.

## Affected Areas
- `backend/src/services/quotaGuardService.js`

## Expected Outcome
Scope the rate limit query by user: join `projects p ON pl.project_id = p.id WHERE p.user_id = ?` (matching the tenant-isolation pattern used in `getQuotaSnapshot`), ensuring that one user's quota exhaustion does not throttle another user's independent API keys.

## Constraints
Do not break existing provider log inserts or queries.

## Suggested Verification
`cd backend && node -e "import('./src/services/quotaGuardService.js')"`

## Status
PENDING

# TASK-032

## Title
Cache directory entries in dubRender audio resolver to eliminate repeated synchronous filesystem scans

## Type
performance

## Priority
low

## Autonomy
auto

## Evidence
[`dubRender.js:87-92`](file:///D:/E/Video_AI/backend/src/pipeline/stages/dubRender.js#L87-L92): `scanForSegment(segmentId)` executes `fs.readdirSync(segDir)` on every invocation. During segment-to-audio resolution, it is called repeatedly for every alignment and fallback row (lines 101, 110), resulting in quadratic synchronous directory traversal operations against disk.

## Affected Areas
- `backend/src/pipeline/stages/dubRender.js`

## Expected Outcome
Read `fs.readdirSync(segDir)` once into a memory array or map before scanning, and filter against the cached entries rather than reading from disk on every segment check.

## Constraints
Ensure filename filtering (`starts with seg_fit_` and `ends with .wav`) remains unchanged.

## Suggested Verification
`cd backend && node -e "import('./src/pipeline/stages/dubRender.js')"`

## Status
PENDING

FINAL_STATUS: SCAN_DONE
