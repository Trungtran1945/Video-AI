# Autonomous Project Scan

## Scan Time
2026-09-18T01:10:00Z

## Project Health
The Video_AI platform exhibits strong test coverage across dub translation gates, CJK validation, and pipeline concurrency guards. However, critical runtime defects exist in the FFmpeg summary pipeline where SQL queries reference non-existent columns (`subtitles.created_date`), along with security gaps in email notification HTML interpolation and password reset session revocation. Storage cleanup for resumable upload sessions also leaves orphaned directories across completed uploads.

## Findings

### BUG
- [backend/src/pipeline/stages/summaryRender.js:54](file:///D:/E/Video_AI/backend/src/pipeline/stages/summaryRender.js#L54) â€” `SELECT storage_key FROM subtitles WHERE project_id = ? ORDER BY created_date DESC` fails on SQLite because table `subtitles` (`schema.js:184-192`) does not have a `created_date` column.
- [backend/src/routes/v1/outputs.js:54](file:///D:/E/Video_AI/backend/src/routes/v1/outputs.js#L54) â€” `id: crypto.randomUUID()` is called without importing `crypto` from `node:crypto`.
- [backend/src/routes/v1/settings.js:10-24](file:///D:/E/Video_AI/backend/src/routes/v1/settings.js#L10-L24) â€” `ALLOWED` fields in `settings.js` omit `active_translate_provider`, discarding user translation provider preferences despite database column (`schema.js:70`) and provider registry support (`registry.js:112`).

### SECURITY
- [backend/src/queue/workers/notifyWorker.js:46-52](file:///D:/E/Video_AI/backend/src/queue/workers/notifyWorker.js#L46-L52) â€” User-controlled `projectTitle` is interpolated unescaped into HTML email notification body, allowing HTML injection and stored XSS in emails.
- [backend/src/routes/v1/auth.js:131-148](file:///D:/E/Video_AI/backend/src/routes/v1/auth.js#L131-L148) â€” `POST /api/v1/auth/reset-password` resets password hash but never calls `clearRefreshToken(user.id)`, failing to invalidate existing active refresh tokens upon password reset.

### PERFORMANCE
- [backend/src/media/ffmpeg.js:66](file:///D:/E/Video_AI/backend/src/media/ffmpeg.js#L66) â€” `setTimeout(() => { try { child.kill('SIGKILL') } catch (_) {} }, 5000)` creates an unreferenced, uncleared timer that keeps the Node.js event loop active after child process close.
- [backend/src/routes/v1/upload.js:46-54](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L46-L54) â€” Resumable upload leaves orphaned session directory in `storage/tmp/upload_sessions/<sessionId>` after renaming the binary file in `/complete`.

### TEST
- [backend/tests/](file:///D:/E/Video_AI/backend/tests/) â€” Missing integration coverage for `summaryRender` subtitle resolution and `notifyWorker` email generation.

### REFACTOR
- [backend/src/lib/crypto.js:28](file:///D:/E/Video_AI/backend/src/lib/crypto.js#L28) â€” `decrypt(payload)` throws unhandled exception on auth tag failure or invalid key, crashing `GET /api/v1/api-keys` (500) rather than returning null or preview placeholder.

### FEATURE OPPORTUNITY
- [frontend/src/pages/ProviderSettings.jsx:10-16](file:///D:/E/Video_AI/frontend/src/pages/ProviderSettings.jsx#L10-L16) â€” Add Translation Provider section (`active_translate_provider`) to allow users to configure translation provider preferences (Gemini vs Google Translate) alongside LLM/TTS.

## Recommended Tasks
1. TASK-008: Fix summaryRender query failure on subtitles created_date column
2. TASK-009: Sanitize project title in notifyWorker email notification to prevent HTML injection
3. TASK-010: Invalidate refresh tokens upon password reset in auth router
4. TASK-011: Clean up orphaned upload session directory on upload completion
5. TASK-012: Add active_translate_provider to allowed settings in settings router

## Final Status
FINAL_STATUS: SCAN_DONE

---

# TASK-008

## Title
Fix summaryRender query failure on subtitles created_date column

## Type
bug

## Priority
critical

## Autonomy
extra-verification

## Evidence
[backend/src/pipeline/stages/summaryRender.js:54](file:///D:/E/Video_AI/backend/src/pipeline/stages/summaryRender.js#L54): `summaryRender` queries `SELECT storage_key FROM subtitles WHERE project_id = ? ORDER BY created_date DESC`. In [backend/src/db/schema.js:184-192](file:///D:/E/Video_AI/backend/src/db/schema.js#L184-L192), the `subtitles` table has columns `id`, `project_id`, `format`, `language`, `storage_key`, `cues` without `created_date`. Executing `summaryRender` causes SQLite to throw `SqliteError: no such column: created_date`, failing the summary pipeline at the final stage.

## Affected Areas
- `backend/src/db/schema.js`
- `backend/src/pipeline/stages/summaryRender.js`
- `backend/src/pipeline/stages/summarySubtitle.js`

## Expected Outcome
1. `initSchema()` in `schema.js` defines `created_date TEXT DEFAULT (datetime('now'))` on `subtitles` table and includes an `ALTER TABLE subtitles ADD COLUMN created_date TEXT DEFAULT (datetime('now'))` migration block.
2. `summarySubtitle.js` populates `created_date` upon inserting new subtitle rows.
3. `summaryRender.js` safely queries subtitles ordered by `created_date DESC` or falls back to `rowid DESC`.

## Constraints
Ensure migration does not fail on existing databases. Do not alter existing schema fields for other tables.

## Suggested Verification
`cd backend && node --test tests/e2ePipeline30.test.mjs`

## Status
PENDING

---

# TASK-009

## Title
Sanitize project title in notifyWorker email notification to prevent HTML injection

## Type
security

## Priority
high

## Autonomy
extra-verification

## Evidence
[backend/src/queue/workers/notifyWorker.js:46-52](file:///D:/E/Video_AI/backend/src/queue/workers/notifyWorker.js#L46-L52): `projectTitle` provided by user input is concatenated directly into raw HTML template:
`<p>Project <strong>${projectTitle}</strong> (${mode}) Ä‘Ã£ ${statusText}.</p>`
without escaping HTML characters (`<`, `>`, `&`, `"`, `'`). This allows an attacker to inject arbitrary HTML or phishing links into outgoing notification emails sent to users.

## Affected Areas
- `backend/src/queue/workers/notifyWorker.js`

## Expected Outcome
All user-controlled fields (`projectTitle`, `mode`, `statusText`) are properly sanitized/escaped before being interpolated into HTML email contents and subject line.

## Constraints
Maintain Vietnamese notification formatting and email delivery flow without modifying nodemailer configuration.

## Suggested Verification
`cd backend && node -e "import('./src/queue/workers/notifyWorker.js')"`

## Status
PENDING

---

# TASK-010

## Title
Invalidate refresh tokens upon password reset in auth router

## Type
security

## Priority
high

## Autonomy
auto

## Evidence
[backend/src/routes/v1/auth.js:131-148](file:///D:/E/Video_AI/backend/src/routes/v1/auth.js#L131-L148): In `POST /api/v1/auth/reset-password`, the user's password hash is updated and the reset token marked used, but existing active refresh tokens are not wiped (`clearRefreshToken(user.id)` is never invoked). Any sessions or refresh tokens retained by compromised clients remain fully functional after password reset.

## Affected Areas
- `backend/src/routes/v1/auth.js`

## Expected Outcome
When a password is successfully reset, `await clearRefreshToken(user.id)` is called to immediately revoke all outstanding refresh tokens and terminate existing sessions.

## Constraints
Must not disrupt existing login or token issuance flows.

## Suggested Verification
`cd backend && node -e "import('./src/routes/v1/auth.js')"`

## Status
PENDING

---

# TASK-011

## Title
Clean up orphaned upload session directory on upload completion

## Type
bug

## Priority
medium

## Autonomy
auto

## Evidence
[backend/src/routes/v1/upload.js:46-54,147](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L46-L54): During chunked upload initialization and chunk streaming, a dedicated temporary folder is created at `storage/tmp/upload_sessions/<sessionId>`. In `POST /api/v1/uploads/:id/complete`, `fs.promises.rename(tmpPath, finalAbs)` moves the binary chunk file to `storage/uploads/`, but leaves the empty session folder intact. Repeated uploads leave thousands of abandoned directories in `storage/tmp/upload_sessions/`.

## Affected Areas
- `backend/src/routes/v1/upload.js`

## Expected Outcome
Upon successful completion and rename of the uploaded file, `fs.promises.rm(sessionDir(session.id), { recursive: true, force: true })` cleans up the temporary session directory.

## Constraints
Directory removal must be best-effort and must not abort or fail a completed upload response.

## Suggested Verification
`cd backend && node --test tests/uploadComplete.test.mjs`

## Status
PENDING

---

# TASK-012

## Title
Add active_translate_provider to allowed settings in settings router

## Type
bug

## Priority
medium

## Autonomy
auto

## Evidence
[backend/src/routes/v1/settings.js:10-24](file:///D:/E/Video_AI/backend/src/routes/v1/settings.js#L10-L24): `ALLOWED` settings list contains `active_llm_provider`, `active_image_provider`, `active_video_provider`, `active_voice_provider`, `active_subtitle_provider`, but omits `active_translate_provider`. The column is present in SQLite (`schema.js:70`) and queried by `providers/registry.js:112`. When a user attempts to configure `active_translate_provider` via `PUT /api/v1/settings`, `normalize()` strips the parameter and the setting is never persisted.

## Affected Areas
- `backend/src/routes/v1/settings.js`

## Expected Outcome
`active_translate_provider` is included in the `ALLOWED` array of `settings.js`, allowing users to persist their preferred translation provider (e.g. `google_translate` vs `gemini`).

## Constraints
Preserve all existing allowed fields and normalization behavior.

## Suggested Verification
`cd backend && node -e "import('./src/routes/v1/settings.js')"`

## Status
PENDING

---

FINAL_STATUS: SCAN_DONE
