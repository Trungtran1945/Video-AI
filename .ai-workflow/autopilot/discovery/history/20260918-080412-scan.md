# Autonomous Project Scan

## Scan Time
2026-09-18T01:04:00Z

## Project Health
The Video_AI codebase exhibits a modular, resilient architecture with stage-based pipeline runners, provider fallbacks, and comprehensive media processing pipelines. However, several concurrency and robustness issues exist in pipeline lifecycle management and data security. Specifically, `runPipeline` suffers from an asynchronous race condition allowing concurrent duplicate executions, key decryption fails hard on authentication mismatch without graceful fallback, temporary upload session folders leak on completion, and user authentication lacks password length constraints.

## Findings

### BUG
- [backend/src/pipeline/runner.js:457-469](file:///D:/E/Video_AI/backend/src/pipeline/runner.js#L457-L469) â€” `activeRuns.add(projectId)` is called after asynchronous `await queryOne()`, enabling race conditions where concurrent calls run overlapping pipelines.
- [backend/src/routes/v1/projects.js:311-326](file:///D:/E/Video_AI/backend/src/routes/v1/projects.js#L311-L326) â€” `POST /api/v1/projects/:id/regenerate` does not verify `isPipelineRunning()`, risking uncoordinated re-execution.
- [backend/src/routes/v1/generation.js:70-92](file:///D:/E/Video_AI/backend/src/routes/v1/generation.js#L70-L92) â€” `POST /api/v1/projects/:id/jobs/:type/retry` does not check `isPipelineRunning()`, unlike the `/redub` endpoint.
- [backend/src/lib/crypto.js:21-30](file:///D:/E/Video_AI/backend/src/lib/crypto.js#L21-L30) â€” `decrypt()` does not catch `decipher.final()` GCM authentication tag failures, throwing unhandled exceptions on corrupted keys.
- [backend/src/routes/v1/apiKeys.js:21](file:///D:/E/Video_AI/backend/src/routes/v1/apiKeys.js#L21) â€” `GET /api/v1/api-keys` crashes with 500 if any stored key fails decryption verification.
- [backend/src/routes/v1/outputs.js:54](file:///D:/E/Video_AI/backend/src/routes/v1/outputs.js#L54) â€” `crypto.randomUUID()` is called in `POST /api/v1/outputs/:id/youtube` without importing `crypto` or `uuidv4`.

### SECURITY
- [backend/src/routes/v1/auth.js:24-27](file:///D:/E/Video_AI/backend/src/routes/v1/auth.js#L24-L27) & [backend/src/routes/v1/auth.js:133-134](file:///D:/E/Video_AI/backend/src/routes/v1/auth.js#L133-L134) â€” Registration and password reset lack password length validation (minimum 8 characters).

### PERFORMANCE
- [backend/src/routes/v1/projects.js:167-180](file:///D:/E/Video_AI/backend/src/routes/v1/projects.js#L167-L180) â€” Sequential single-row inserts for transcript segment duplication during cache hits instead of batched inserts.

### TEST
- [backend/tests/resumeOrder.test.mjs:1-81](file:///D:/E/Video_AI/backend/tests/resumeOrder.test.mjs#L1-L81) â€” Stage resolution is tested for order, but missing concurrency tests verifying that multiple simultaneous `runPipeline` calls are deduplicated.

### REFACTOR
- [backend/src/routes/v1/upload.js:46-50](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L46-L50) & [backend/src/routes/v1/upload.js:147](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L147) â€” Completed upload renames `blob.bin` into `storage/uploads/` but leaves empty `storage/tmp/upload_sessions/<sessionId>` directories orphaned.

### FEATURE OPPORTUNITY
- [backend/src/routes/v1/upload.js:82-124](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L82-L124) â€” Resumable upload lacks an explicit `DELETE /api/v1/uploads/:id` cancellation endpoint to clean up abandoned partial chunks.

## Recommended Tasks
1. TASK-008 Fix race condition in runPipeline and add running guards to regenerate and retry endpoints
2. TASK-009 Catch decryption authentication failures in crypto.decrypt to prevent 500 crashes
3. TASK-010 Clean up upload session directories on completion and add cancellation cleanup
4. TASK-011 Enforce minimum password length in auth registration and password reset

## Final Status
FINAL_STATUS: SCAN_DONE

---

# TASK-008

## Title
Fix race condition in runPipeline and add running guards to regenerate and retry endpoints

## Type
bug

## Priority
high

## Autonomy
extra-verification

## Evidence
[runner.js:457-469](file:///D:/E/Video_AI/backend/src/pipeline/runner.js#L457-L469): `activeRuns.add(projectId)` is invoked after `await queryOne('SELECT * FROM projects WHERE id = ?', [projectId])`. Two concurrent requests yield to the event loop on `await queryOne` and both proceed past `if (activeRuns.has(projectId)) return`, running duplicate pipelines simultaneously. Additionally, [projects.js:311-326](file:///D:/E/Video_AI/backend/src/routes/v1/projects.js#L311-L326) (`POST /:id/regenerate`) and [generation.js:70-92](file:///D:/E/Video_AI/backend/src/routes/v1/generation.js#L70-L92) (`POST /:id/jobs/:type/retry`) do not check `isPipelineRunning()`, returning success status even if a run is already active.

## Affected Areas
- `backend/src/pipeline/runner.js`
- `backend/src/routes/v1/projects.js`
- `backend/src/routes/v1/generation.js`

## Expected Outcome
1. `activeRuns.add(projectId)` is executed synchronously at the start of `runPipeline` before any `await`. If subsequent project queries fail or project does not exist, `activeRuns.delete(projectId)` cleans up in a `finally` block.
2. `POST /projects/:id/regenerate` and `POST /projects/:id/jobs/:type/retry` verify `isPipelineRunning(projectId)` and return 409 `PIPELINE_RUNNING` if the pipeline is actively running.

## Constraints
Do not alter stage execution order or change the interface of `isPipelineRunning`.

## Suggested Verification
`cd backend && node --test tests/resumeOrder.test.mjs`

## Status
PENDING

# TASK-009

## Title
Catch decryption authentication failures in crypto.decrypt to prevent 500 crashes

## Type
bug

## Priority
medium

## Autonomy
auto

## Evidence
[crypto.js:21-30](file:///D:/E/Video_AI/backend/src/lib/crypto.js#L21-L30): `decrypt()` calls `decipher.final()` without wrapping it in a try-catch block. In AES-256-GCM, if ciphertext is corrupted or `MASTER_KEY` differs, `decipher.final()` throws `Unsupported state or unable to authenticate data`. In [apiKeys.js:21](file:///D:/E/Video_AI/backend/src/routes/v1/apiKeys.js#L21), `GET /api/v1/api-keys` iterates over stored keys and invokes `decrypt()` directly, causing the entire endpoint to fail with an unhandled 500 error.

## Affected Areas
- `backend/src/lib/crypto.js`
- `backend/src/routes/v1/apiKeys.js`

## Expected Outcome
`decrypt(payload)` catches errors thrown by `decipher.final()`, logs a warning with redacted content, and returns `null` instead of throwing an unhandled exception. Endpoints handling keys gracefully handle null decrypted values.

## Constraints
Maintain standard output format for valid encrypted payloads. Do not log decrypted plaintexts or raw master keys.

## Suggested Verification
`cd backend && node --test tests/providerErrors.test.mjs`

## Status
PENDING

# TASK-010

## Title
Clean up upload session directories on completion and add cancellation cleanup

## Type
refactor

## Priority
medium

## Autonomy
auto

## Evidence
[upload.js:46-50](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L46-L50) and [upload.js:147](file:///D:/E/Video_AI/backend/src/routes/v1/upload.js#L147): When a chunked upload completes, `fs.promises.rename(tmpPath, finalAbs)` moves the binary file `blob.bin` to `storage/uploads/`, but the enclosing directory `storage/tmp/upload_sessions/<sessionId>` is left behind on disk. Over time, empty session folders accumulate.

## Affected Areas
- `backend/src/routes/v1/upload.js`

## Expected Outcome
1. `POST /api/v1/uploads/:id/complete` removes the temporary session directory via `fs.promises.rm(sessionDir, { recursive: true, force: true })` after successful relocation of `blob.bin`.
2. A `DELETE /api/v1/uploads/:id` route is provided to let users abort an incomplete upload session and purge its temporary files immediately.

## Constraints
Do not delete the moved final asset in `storage/uploads/`. Handle missing directories gracefully with `force: true`.

## Suggested Verification
`cd backend && node --test tests/uploadComplete.test.mjs`

## Status
PENDING

# TASK-011

## Title
Enforce minimum password length in auth registration and password reset

## Type
security

## Priority
medium

## Autonomy
extra-verification

## Evidence
[auth.js:24-27](file:///D:/E/Video_AI/backend/src/routes/v1/auth.js#L24-L27) and [auth.js:133-134](file:///D:/E/Video_AI/backend/src/routes/v1/auth.js#L133-L134): `POST /api/v1/auth/register` and `POST /api/v1/auth/reset-password` only check for truthiness of `password` and `newPassword` (e.g. `!password`). Users can register accounts with 1-character passwords.

## Affected Areas
- `backend/src/routes/v1/auth.js`

## Expected Outcome
Both registration and password reset validate that the provided password has a minimum length of 8 characters, returning `400 Bad Request` with `ERR.VALIDATION` if shorter.

## Constraints
Do not break existing user logins. Only enforce minimum length during new registration and password reset requests.

## Suggested Verification
`cd backend && node --test tests/cacheIdentity.test.mjs`

## Status
PENDING

FINAL_STATUS: SCAN_DONE
